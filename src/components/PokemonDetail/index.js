import { Button } from "@mui/material";

const PokemonDetail = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Detalle del pokemon
      </Button>
    </div>
  );
};

export default PokemonDetail;
