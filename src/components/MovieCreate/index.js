import { Button, Dialog, DialgContent, TextField, Grid } from "@mui/material";

const MovieCreate = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Crear Pelicula
      </Button>
    </div>
  );
};

export default MovieCreate;
