import { Container, Grid } from "@mui/material";

const Map = () => {
  return (
    <Container maxWidth="lg">
      <Grid>
        <h1>Mapas</h1>
      </Grid>
    </Container>
  );
};

export default Map;
